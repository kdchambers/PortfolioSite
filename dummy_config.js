/* 
 This is just a dummy config to keep private data secret
 
 Fill in parameters and rename to 'config.js'
 */

const config = {
	forwardEmailAddress: 'test@test.com',
	forwardEmailPassword: 'password',
	targetEmailAddress: 'target@target.com',
	apiPort: 80
};

default export {
	config
};
